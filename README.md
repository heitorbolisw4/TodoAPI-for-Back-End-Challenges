# TODO API for back-end challenge


Routes

POST /register - For User register
POST /auth - for User Authentication


GET /todos - Return All tasks linked to the user
POST /todos - Create a Task
PUT /todos/:id - Update a Task by Id
Patch /todos/status/:id - Update a status task by id
# TodoAPI-for-Back-End-Challenges
